# Server-side secret used by server/index.ts routes only.
GEMINI_API_KEY=""

# Optional: explicit backend URL for the browser client.
# Leave empty in local development to use Vite proxy (/api -> http://localhost:8787).
VITE_API_BASE_URL=""

# Optional runtime URL used by hosting/deployment.
APP_URL=""
